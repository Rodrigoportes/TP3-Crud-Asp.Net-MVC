@model IEnumerable<TP3Crud.Models.Especializacao>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Horas)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Encarregado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Horas)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Encarregado.EncarregadoId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EspecializacaoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.EspecializacaoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.EspecializacaoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
